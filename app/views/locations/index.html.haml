%h1 Listing locations

%table
  %thead
    %tr
      %th Rush
      %th Name
      %th Description
      %th Lat
      %th Long
      %th
      %th
      %th

  %tbody
    - @locations.each do |location|
      %tr
        %td= location.rush_id
        %td= location.name
        %td= location.description
        %td= location.lat
        %td= location.long
        %td= link_to 'Show', rush_location_path(location, location.rush)


